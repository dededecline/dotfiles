# Kitty Terminal Configuration

# Shell
shell /opt/homebrew/bin/fish

# Scrollback
scrollback_lines 10000

# Audio
enable_audio_bell no

# Window appearance
background_opacity 0.55
background_blur 64
window_padding_width 20
hide_window_decorations titlebar-only

# Cursor configuration
cursor_shape underline
cursor_trail 4
cursor_blink_interval 0

# Tab bar customization
tab_bar_style separator
tab_bar_edge top
tab_bar_align left
tab_separator " "
active_tab_font_style italic
tab_title_template "╭─  ◆{index} {title[:16]} ─╮"
tab_activity_symbol " ✦"
tab_bar_min_tabs 1

# Font settings
font_family Maple Mono NF
bold_font auto
italic_font auto
bold_italic_font auto
font_size 12.0

# Enable ligatures
disable_ligatures never

# Remote control
allow_remote_control yes

# Window close confirmation
confirm_os_window_close 0

# Keybindings
# Faster cursor movement (word-by-word with Option, line start/end with Cmd)
map alt+left send_text all \x1bb
map alt+right send_text all \x1bf
map cmd+left send_text all \x01
map cmd+right send_text all \x05

map cmd+f launch --type=overlay --stdin-source=@screen_scrollback /opt/homebrew/bin/fzf --no-sort --no-mouse --exact -i --tac
map cmd+1 goto_tab 1
map cmd+2 goto_tab 2
map cmd+3 goto_tab 3
map cmd+4 goto_tab 4
map cmd+5 goto_tab 5
map cmd+6 goto_tab 6
map cmd+7 goto_tab 7
map cmd+8 goto_tab 8
map cmd+9 goto_tab 9

mouse_map left click grabbed mouse_handle_click link

# Include Catppuccin Frappe theme
# Source: ~/.config/.system/themes/catppuccin-frappe/
include ../.system/themes/catppuccin-frappe/kitty.conf
