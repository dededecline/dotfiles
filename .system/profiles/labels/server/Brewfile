# =============================================================================
# Remote Access
# =============================================================================
brew "tailscale"                # Mesh VPN (CLI — required for Tailscale SSH server)
