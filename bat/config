# Bat Configuration

# Theme managed by symlinks.sh from .system/themes/
# Run `bat cache --build` after changes
--theme="Catppuccin Frappe"

# Disable line numbers
--style=-numbers

# Disable pager and output full file directly
--paging=never
