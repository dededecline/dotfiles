# LSD Configuration

# Color theme (use custom theme file)
color:
  theme: custom

# Icons
icons:
  theme: fancy
  separator: " "

# Show column headers
header: true

# Enable hyperlinks
hyperlink: always
